"""MCP server setup — tool, resource, and prompt registration."""


async def main() -> None:
    """Start the OpenSCAD MCP server on stdio transport.

    Full implementation in task 15.
    """
    raise NotImplementedError("Server not yet wired — see task 15")
